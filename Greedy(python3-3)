//min() 함수를 이용 -> 리스트에서 가장 작은 원소를 찾아주는 함수
//map 함수 -> map(적용할 함수, 반복 가능한 자료형)
//split() -> 띄어쓰기를 기준으로 구분해주는 함수
// input() -> 입력되는 값을 문자열로 취급

n, m = map(int, input().split()) 

result = 0

for i in range(n):
  data = list(map(int, input().split())
  
  // 현재 줄에서 가장 작은 수 찾기
  min_value = min(data) 
  
  // 가장 작은 수들 중에서 가장 큰 수 찾기
  result = max(result, min_value)
}

print(result)
